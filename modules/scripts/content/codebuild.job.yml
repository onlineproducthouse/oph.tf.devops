version: 0.2

# JOB_SCRIPT_STORE_URL

phases:
  install:
    commands:
      - apt-get update -y
      - apt-get install -y jq zip

  build:
    commands:
      - JOB_SCRIPT=./job.sh
      - aws s3 cp $JOB_SCRIPT_STORE_URL $JOB_SCRIPT
      - bash $JOB_SCRIPT

artifacts:
  files:
    - '**/*'
